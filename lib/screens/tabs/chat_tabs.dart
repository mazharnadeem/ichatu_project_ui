

import 'package:flutter/material.dart';

class ChatTabs extends StatelessWidget {
  const ChatTabs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Mazhar Nadeem Chat'),
    );
  }
}
